# SPDX-License-Identifier: GPL-2.0
#
# qqcandy: module-only probe with a parser shared by the host tests.
# It uses the setup_arch LKINFO stash, not the built-in ccci_util parser.

obj-$(CONFIG_MTK_CCCI_PROBE_MODULE) += ccci_probe.o
