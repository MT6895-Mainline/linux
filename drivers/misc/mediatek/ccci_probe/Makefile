# SPDX-License-Identifier: GPL-2.0
#
# qqcandy: self-contained loadable probe. The module source carries the
# minimal tag structs it needs, so it does not link against built-in
# ccci_util symbols.

obj-$(CONFIG_MTK_CCCI_PROBE_MODULE) += ccci_probe.o
