# SPDX-License-Identifier: GPL-2.0
mainmenu "CCCI probe module-only tests"

config MODULES
	bool "Modules"
	modules

config ARCH_MEDIATEK
	bool "MediaTek"

config MTK_EMBED_BOARD_DTB
	bool "Embedded board DTB"

# Stub for the builtin util both dangerous modules depend on; the matrix
# below also covers the util missing (=n) and module (=m) cases.
config MTK_CCCI_UTIL
	tristate "CCCI util (stub)"
	default n

source "drivers/misc/mediatek/ccci_probe/Kconfig"
source "drivers/misc/mediatek/ccci_smem_dump/Kconfig"
source "drivers/misc/mediatek/ccci_ccif/Kconfig"
